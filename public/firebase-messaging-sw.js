/**
 * Firebase Messaging Service Worker
 * This file exists for Firebase SDK compatibility.
 * It imports the main service worker (sw.js) which handles all Firebase messaging.
 */
importScripts('/sw.js');
